# Frontend

React (Vite) SPA for the POS system. See the [root README](../README.md) for full
setup instructions, environment variables, and feature list.

```bash
npm install
npm run dev   # http://localhost:5173, proxies /api and /uploads to the backend
```
